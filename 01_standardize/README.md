## Standardize Data

The scripts in this folder perform necessary site-specific wrangling to get all data files into a comparable format. Each file assumes you have downloaded the necessary inputs from the Environmental Data Initiative (see the scripts in `00_download/`).
